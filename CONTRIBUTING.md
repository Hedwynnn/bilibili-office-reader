# 参与贡献

感谢你愿意帮助改进 Bilibili Office Reader。

## 提交问题

请先搜索现有 Issue，避免重复提交。Bug 报告尽量包含：

- 脚本版本；
- 浏览器及版本；
- Tampermonkey 或 Violentmonkey 版本；
- 可重复的操作步骤；
- 预期结果与实际结果；
- 脱敏后的控制台错误或截图。

请勿提交 Cookie、密码、访问令牌或包含个人动态内容的未打码截图。

## 提交代码

1. Fork 仓库并从 `main` 创建短期分支。
2. 保持修改聚焦，不顺带重构无关代码。
3. 若修改可测试逻辑，请补充或更新测试。
4. 运行 `npm run verify` 或 `pnpm run verify`。
5. 在 Pull Request 中说明原因、行为变化和验证方式。

功能建议可以先开 Issue 讨论。项目以低干扰文字阅读为核心，不保证接受所有扩展功能。
